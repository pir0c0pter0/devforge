/**
 * Telegram Bot Session Management
 *
 * This module will export session management utilities for the Telegram bot.
 * Sessions are stored per-user and track selected container, activity, etc.
 *
 * Planned features:
 * - Session persistence (Redis/SQLite backing store)
 * - Session cleanup for inactive users
 * - Session migration utilities
 */

export {}
